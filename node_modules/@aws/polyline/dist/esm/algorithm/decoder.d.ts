import { CompressionParameters } from "../polyline-types";
export declare class PolylineDecoder {
    readonly decodingTable: number[];
    readonly containsHeader: boolean;
    constructor(decodingTable: number[], containsHeader: boolean);
    private decodeSignedValue;
    private decodeUnsignedValue;
    private decodeHeader;
    decode(encoded: string, encodePrecision?: number): [lngLatArray: Array<Array<number>>, header: CompressionParameters];
}
