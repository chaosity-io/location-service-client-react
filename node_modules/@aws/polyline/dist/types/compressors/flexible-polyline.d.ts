import { DataCompressor } from "../data-compressor";
import { CompressionParameters } from "../polyline-types";
import { PolylineEncoder } from "../algorithm/encoder";
import { PolylineDecoder } from "../algorithm/decoder";
export declare class FlexiblePolyline extends DataCompressor {
    readonly DataContainsHeader = true;
    readonly FlexPolylineEncodingTable = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789-_";
    readonly FlexPolylineDecodingTable: number[];
    readonly encoder: PolylineEncoder;
    readonly decoder: PolylineDecoder;
    constructor();
    compressLngLatArray(lngLatArray: Array<Array<number>>, parameters: CompressionParameters): string;
    decompressLngLatArray(encodedData: string): [Array<Array<number>>, CompressionParameters];
}
