import { DataCompressor } from "../data-compressor";
import { CompressionParameters } from "../polyline-types";
import { PolylineEncoder } from "../algorithm/encoder";
import { PolylineDecoder } from "../algorithm/decoder";
declare abstract class EncodedPolyline extends DataCompressor {
    readonly precision: number;
    readonly DataContainsHeader = false;
    readonly PolylineEncodingTable: string;
    readonly PolylineDecodingTable: number[];
    readonly encoder: PolylineEncoder;
    readonly decoder: PolylineDecoder;
    constructor(precision: number);
    compressLngLatArray(lngLatArray: Array<Array<number>>): string;
    decompressLngLatArray(compressedData: string): [Array<Array<number>>, CompressionParameters];
}
export declare class Polyline5 extends EncodedPolyline {
    constructor();
}
export declare class Polyline6 extends EncodedPolyline {
    constructor();
}
export {};
