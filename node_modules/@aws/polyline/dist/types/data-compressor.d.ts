import { LineString, Polygon, Feature } from "geojson";
import { CompressionParameters } from "./polyline-types";
export declare abstract class DataCompressor {
    protected abstract compressLngLatArray(lngLatArray: Array<Array<number>>, parameters: CompressionParameters): string;
    protected abstract decompressLngLatArray(compressedData: string): [Array<Array<number>>, CompressionParameters];
    private polygonIsCounterClockwise;
    private positionsAreEquivalent;
    private decodeLineString;
    private decodePolygon;
    private compressionParametersToGeoJsonProperties;
    encodeFromLngLatArray(lngLatArray: Array<Array<number>>, parameters: CompressionParameters): string;
    decodeToLngLatArray(compressedData: string): Array<Array<number>>;
    decodeToLineString(compressedData: string): LineString;
    decodeToPolygon(compressedData: Array<string>): Polygon;
    decodeToLineStringFeature(compressedData: string): Feature;
    decodeToPolygonFeature(compressedData: Array<string>): Feature;
}
