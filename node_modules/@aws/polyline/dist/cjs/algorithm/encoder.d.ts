export declare class PolylineEncoder {
    readonly encodingTable: string;
    readonly includeHeader: boolean;
    constructor(encodingTable: string, includeHeader: boolean);
    private polylineRound;
    encode(lngLatArray: Array<Array<number>>, precision: number, thirdDim?: number, thirdDimPrecision?: number): string;
    private encodeHeader;
    private encodeUnsignedValue;
    private encodeSignedValue;
}
