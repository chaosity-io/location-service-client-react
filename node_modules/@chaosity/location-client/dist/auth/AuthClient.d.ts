import { OAuth2Credentials, TokenResponse } from '../types';
export declare class AuthClient {
    private authEndpoint;
    constructor(authEndpoint?: string);
    fetchToken(credentials: OAuth2Credentials): Promise<TokenResponse>;
}
